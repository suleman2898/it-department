This folder is included to satisfy the requested lowercase project structure.

Actual SQLite storage is intentionally placed in the application root as:
data.db

This avoids common SQLite path errors in PyInstaller EXE mode.